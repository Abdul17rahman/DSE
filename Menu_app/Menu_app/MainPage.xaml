<Page
    x:Class="Menu_app.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Menu_app"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="#254658">

        <CommandBar>
            <AppBarButton Icon="Admin"
                          x:Name="admin"
                          Label="Admin"/>

            <AppBarButton Icon="Account"
                          x:Name="account"
                          Label="Account"/>

            <AppBarButton Icon="Home"
                          x:Name="home"
                          Label="Home" Click="home_appbar_clcik"/>
        </CommandBar>
        
        <Button x:Name="btncommand"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Content="Command View"
                Padding="20"
                Foreground="#FFF"
                FontSize="30"
                FontWeight="Bold"
                CornerRadius="15" Click="view_output_click"/>
    </Grid>
</Page>
